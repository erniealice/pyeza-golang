.DS_Store
.claude/

# Test artifacts
node_modules/
playwright-report/
test-results/

# SDK build artifacts (@leapfor/ui dual-build) — regenerated by `pnpm build`.
# dist/ holds lf.esm.js + lf.umd.cjs + table.bundle.js + lf.d.ts; never
# source. The running app does NOT consume these (it loads the per-file IIFE
# scripts copied verbatim by pyeza.CopyStaticAssets).
dist/

# Prevent accidental nested clone
pyeza-golang/
